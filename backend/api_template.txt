
from flask import jsonify, request, Blueprint

name_bp = Blueprint('name',__name__)


# function with no paremeter
@name_bp.route('/api/name', methods=['GET'])
# function with some paremeters
@name_bp.route('/api/name', methods=['POST'])
# define function
def name():
    # return 要轉成json
    

# 去 app.py 加上下面這行指令
app.register_blueprint(name_bp)

    